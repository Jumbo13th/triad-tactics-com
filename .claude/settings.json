{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "output=$(npx tsc --noEmit 2>&1 | grep -v '.next-dev/' | head -30); if [ -n \"$output\" ]; then node -e \"console.log(JSON.stringify({hookSpecificOutput:{hookEventName:'Stop',additionalContext:'tsc --noEmit found type errors. Please fix them:\\n'+process.argv[1]}}))\" \"$output\"; fi",
            "timeout": 120,
            "statusMessage": "Type-checking with tsc..."
          }
        ]
      }
    ]
  }
}
